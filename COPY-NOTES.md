# 项目副本说明

这是从 `little-room-v189-handoff` 当前根目录整理出的独立副本，用于后续二次开发。

已保留：

- 完整前端：`index.html`、`styles.css`、`app.js`、PWA manifest 与 Service Worker
- 前端素材：图标、表情图片、MiSans 字体
- 当前 Node / Vercel API 源码和部署配置
- 项目说明、交接记录与依赖清单

未复制：

- `.data/runtime-secrets.json` 等本机运行数据和密钥
- 历史版本目录、上传包和压缩备份
- 之前误做的作品集介绍页草稿

本副本不会包含浏览器 LocalStorage 中的聊天记录、Memory 或 API 配置。
