# Little Room Handoff

## v571

- 放宽 AI 自主使用手机的动机标准：日常好奇、普通想法、暂时没说的话和确实怕忘的小事，都可以自然触发浏览器、备忘录或待办，不再需要等到严重矛盾或重大事件。
- 仍保持克制：多数 5～20 分钟检查可以选择不使用，刚操作过不久也会倾向暂停，避免机械制造记录。
- 新增“最近一次自主操作”时间，让模型按真实使用间隔判断，而不是只看当前聊天强度。
- `app.js`、`index.html`、`sw.js` 已统一到 `v571`。

## v570

- 调整连续结构化回复的处理：不再只保留最后一份，而是按原顺序合并每一份中的正常聊天内容。
- 两份回复之间夹带的普通文字也会保留；仅移除 `messages`、`sticker`、`voice` 等内部 JSON 外壳。
- 完全相同的消息会去重，不同内容都会正常显示。
- `app.js`、`index.html`、`sw.js` 已统一到 `v570`。

## v569

- 修复模型偶尔连续返回多份结构化回复时，`messages`、`sticker`、`voice` 等内部 JSON 被拆成普通聊天气泡的问题。
- 回复解析会逐个识别完整 JSON，并以最后一份有效回复为准；完整对象损坏时仍会尝试恢复其中的消息数组。
- 新增协议泄漏兜底：无法恢复的内部结构会被拦截，不再直接显示给用户。
- `app.js`、`index.html`、`sw.js` 已统一到 `v569`。

## v568

- 修复仅点击“检查更新”后，聊天模型、网页搜索、Notion、网易云和 MiniMax 等 API 凭据可能一起丢失的问题。
- 检查更新前会把完整 API 配置立即写入主存档，并额外放入跨本次重载的临时恢复区；新版页面启动时优先恢复这份更新前快照。
- API 配置新增独立本地备份；主存档中的敏感字段意外为空时，会从备份恢复。
- 敏感字段只有在用户实际编辑后才会以空值覆盖；浏览器自行清空密码框显示时会保留原值。
- “保存 API”和清空聊天记录改为立即落盘，避免刷新时序冲突。
- `app.js`、`index.html`、`sw.js` 已统一到 `v568`。

## v567

- 完善固定 system prompt：明确“ta 的手机”是小屋内真实保存数据的手机，相关话题默认不按情景扮演理解，所有操作都必须以真实记录或成功结果为准。
- 手机密码仍由 AI 自主选择和更换，但用户明确询问真实当前密码时不能无限拒绝、让用户猜或编造其他数字。
- 新增矛盾与关系修复规则：AI 需要主动理解问题、承担自身造成的伤害并持续修复，不能把“怎么哄、说什么、怎么办”的责任推回给用户，也不能擅自冷处理或结束对话。
- 对话摘要会保留尚未解决的矛盾，不能仅因为 AI 已经道歉就误判为和好。
- `app.js`、`index.html`、`sw.js` 已统一到 `v567`。

## v566

- 刷新私聊底部弹窗资源版本，避免旧的浮层样式被本地缓存继续使用。
- `app.js`、`index.html`、`sw.js` 已统一到 `v566`。

## v565

- 私聊页省略号弹窗改为与“删除单条动态”一致的底部面板样式。
- 面板贴底、顶部圆角、灰色背景、白色操作项，并使用相同强度的遮罩和模糊效果。
- 移除单独的取消按钮，点击遮罩或按返回键关闭。
- `app.js`、`index.html`、`sw.js` 已统一到 `v565`。

## v564

- Diary 页面新增导出入口，可选择导出全部私人日记为 `.txt` 或 `.md`。
- 导出内容按北京时间从旧到新排列，包含每篇日记的完整日期、时间和正文，并带 UTF-8 标记方便手机和电脑打开。
- 没有日记时不会生成空文件，会直接提示。
- `app.js`、`index.html`、`sw.js` 已统一到 `v564`。

## v563

- 私聊页右上角省略号菜单改为从底部滑出的操作弹窗。
- 新增全屏半透明遮罩、底部圆角面板、取消按钮，以及打开和关闭动效。
- 原有“链接”和“清空聊天记录”功能保持不变。
- `app.js`、`index.html`、`sw.js` 已统一到 `v563`。

## v562

- 私聊输入框的表情图标向右平移 `4px`，与添加图标的可见边缘间距调整为约 `10px`。
- 添加图标位置和两个按钮的点击区域保持不变。
- `app.js`、`index.html`、`sw.js` 已统一到 `v562`。

## v561

- 私聊输入框从表情/添加按钮切换到发送按钮时，增加轻微淡入、位移和缩放动效。
- 清空文字切回表情/添加按钮时也使用柔和进入动效，并兼容系统“减少动态效果”设置。
- `app.js`、`index.html`、`sw.js` 已统一到 `v561`。

## v560

- 私聊输入文字后，发送按钮右边缘到输入框右边缘的距离固定为 `15px`。
- 空输入状态的表情与添加按钮位置保持不变。
- `app.js`、`index.html`、`sw.js` 已统一到 `v560`。

## v559

- 私聊输入框的表情按钮替换为用户提供的 `face-slightly-smiling 2.svg`。
- 私聊输入框的添加按钮替换为用户提供的 `Group 3.svg`。
- 两个图标使用原稿 `22 × 22px`，保留现有按钮点击区域和间距。
- `app.js`、`index.html`、`sw.js` 已统一到 `v559`。

## v558

- 私聊输入文字后显示的发送按钮替换为用户提供的 `Frame 14.svg` 矢量样式。
- 保持按钮 `38 × 22px`，继续使用输入框内垂直居中布局。
- `app.js`、`index.html`、`sw.js` 已统一到 `v558`。

## v557

- AI 自主使用手机的前台判断间隔调整为随机 `5～20 分钟`。
- PWA 离开满 `30 分钟` 后，下次打开时开始补算离开期间的手机行为。
- `app.js`、`index.html`、`sw.js` 已统一到 `v557`。

## v556

- 修复私聊发送按钮看起来被压到的问题：输入框聚焦时不再显示浅色 focus 框，发送按钮在输入胶囊内垂直居中。
- `app.js`、`index.html`、`sw.js` 已统一到 `v556`。

## v555

- 私聊有文字时的发送按钮改为使用用户提供的 `Group 1.svg` 原件，保存为 `icons/chat-send-button.svg`，尺寸 38×22。
- `app.js`、`index.html`、`sw.js` 已统一到 `v555`，Service Worker 已缓存发送按钮 SVG。

## v554

- 私聊有文字时的发送按钮由扁胶囊改为 30×30 黑色圆形，图标 17×17，避免在 42px 输入框里显得被压扁。
- `app.js`、`index.html`、`sw.js` 已统一到 `v554`。

## v553

- 私聊输入框有文字时，右侧发送按钮按 `Frame 2.svg` 调整为 38×22 黑色胶囊，内部图标 15×15。
- `app.js`、`index.html`、`sw.js` 已统一到 `v553`。

## v552

- 保留全局 MiSans，但恢复特化字体：Home 便签继续使用 `YunFengHanChanTi` 手写字体，手机锁屏时间继续使用系统/SF 风格字体。
- `app.js`、`index.html`、`sw.js` 已统一到 `v552`。

## v551

- 小屋全局字体切换为 MiSans，并加入 Light、Regular、Medium、Semibold、Bold、Heavy 常用字重。
- 原本单独使用手写字体的 Home 便签、手机锁屏时间等也统一改为 MiSans。
- `app.js`、`index.html`、`sw.js` 已统一到 `v551`，Service Worker 已缓存 MiSans 多字重字体。

## v550

- 私聊顶部标题使用本地 `MiSans-Regular.woff2`，字重 Regular、字号 16px。
- `app.js`、`index.html`、`sw.js` 已统一到 `v550`，Service Worker 已缓存 MiSans 字体。

## v549

- 私聊顶部按用户提供的 `Frame 3.svg` 调整：40px 高、`#F4F6F5` 背景、标题居中，仅保留右侧省略号菜单。
- 私聊输入框外层背景改为透明；输入栏内“添加”图标改为圆圈加号。
- `app.js`、`index.html`、`sw.js` 已统一到 `v549`。

## v548

- 私聊页按 Figma 节点 `128:452` 调整外壳 UI：聊天区域背景改为浅灰 `#ededed`，顶部标题区和底部输入区改为 `#f4f6f5`，输入栏改成 42px 左右的白色圆角胶囊。
- 保留现有私聊按钮、消息结构和主导航；未在 Figma 中明确修改的功能不动。
- `app.js`、`index.html`、`sw.js` 已统一到 `v548`。

## v547

- 新增“打开时补行为”：PWA 重新打开或从后台回来时，会根据离开时长让 ta 补记独处期间可能做过的手机操作，并真实保存到浏览器、备忘录、提醒事项等手机数据里。
- 离线补行为至少离开 2 小时才触发，最多补 3 次，并带 30 分钟冷却；不会在离线补行为里自行更改锁屏密码。
- `app.js`、`index.html`、`sw.js` 已统一到 `v547`。

## v546

- 网页搜索入口和设置页已移除重复副标题、服务说明及 Tavily/OpenRouter Key 申请跳转，只保留必要配置项和保存按钮。
- `app.js`、`index.html`、`sw.js` 已统一到 `v546`。

## v545

- 手机密码页的锁图标与锁屏锁图标使用同一纵向位置；“输入密码”和四个密码点从锁屏日期所在位置自然向下排列。
- 手机内部切换补充 iPhone 风格动效：锁屏进入密码页、密码解锁桌面、桌面打开 App、App 缩回原图标、重新锁屏均有对应缩放与淡入过渡。
- App 打开与关闭会以实际被点击图标为缩放原点；系统开启“减少动态效果”时自动停用这些动画。
- `app.js`、`index.html`、`sw.js` 已统一到 `v545`。

## v544

- ta 的手机“待办”App 已改用 `icons/提醒事项-iOS-512x512.png`，并同步加入离线缓存。
- 独立网页搜索现在可选择 Tavily 或 OpenRouter；OpenRouter 可单独填写 Key 和搜索模型，不要求聊天 API 也使用 OpenRouter。
- 独立 OpenRouter 搜索使用官方 `openrouter:web_search` 服务端工具；原有“聊天 API 本身为 OpenRouter、搜索 Key 留空”的兼容方式继续保留。
- `app.js`、`index.html`、`sw.js` 已统一到 `v544`。

## v543

- 网页搜索新增独立 Tavily 配置入口，不再要求聊天模型必须使用 OpenRouter；聊天和 ta 的手机浏览器共用这套真实搜索能力。若未填写 Tavily Key 且聊天 API 为 OpenRouter，仍兼容原来的 OpenRouter 搜索。
- Home 已移除天气卡片和 Todo；Diary、Book 改为并排展示。
- ta 的手机桌面新增“待办”和“天气”App。旧 Todo 会迁入手机，AI 可在独处时自行添加或完成真实待办；天气信息仍会进入 AI 必要上下文，但不再展示给用户的 Home。
- `app.js`、`index.html`、`sw.js` 已统一到 `v543`。

## v542

- 已移除手机桌面三个 App 图标外层额外的半透明边框、底色、内阴影和模糊效果，只显示 `icons` 文件夹中的原始图标。
- `app.js`、`index.html`、`sw.js` 已统一到 `v542`。

## v541

- Home 页“ta的手机”入口取消蓝色特化图标，改为与 Diary、Book 一致的浅灰底黑色线性图标。
- 已移除手机入口下方的最近使用副标题，只保留标题。
- `app.js`、`index.html`、`sw.js` 已统一到 `v541`。

## v540

- 手机桌面顶栏已与锁屏、密码页保持一致：左侧显示“中国移动”，右侧统一为信号、Wi‑Fi 和 `57` 电量图标。
- 桌面顶栏不再单独显示时间。
- `app.js`、`index.html`、`sw.js` 已统一到 `v540`。

## v539

- 手机桌面的浏览器、备忘录和设置已换成 `icons` 文件夹中的对应原生风格图标。
- 三个图标已加入离线缓存，PWA 离线打开时也会正常显示。
- `app.js`、`index.html`、`sw.js` 已统一到 `v539`。

## v538

- 手机“设置 → 桌面背景”新增照片背景：用户可以从相册选择照片，图片会压缩后真实保存在手机状态中，并同时用于锁屏和桌面。
- 已选择的照片可以继续更换或移除；移除后自动恢复极光背景。
- `app.js`、`index.html`、`sw.js` 已统一到 `v538`。

## v537

- 私聊新增真实的 AI 手机密码操作：用户明确把 ta 自己手机密码的决定权交给 ta 时，AI 可自行选择四位密码并结构化保存，不会只在聊天里假装设置。
- 询问密码、普通聊天、泛泛提到手机、用户直接指定数字都不会触发修改；工具结果会明确区分成功和失败。
- AI 首次获权并设置后，至少间隔 7 天才允许在独处使用手机时自主更换，避免频繁改密码。
- 仅当用户明确询问密码时，当前真实密码才作为必要手机状态提供给 AI；是否告诉用户仍由 AI 按人设决定。
- 设定页新增“手机紧急重置”，无法进入时可重置为 `2580`，并撤销 AI 的持续改密权限，直到用户再次在私聊授权。
- 手动在手机设置中改密码也会撤销 AI 的持续改密权限。
- `app.js`、`index.html`、`sw.js` 已统一到 `v537`。

## v536

- 已移除密码页的“紧急情况”，底部仅保留右侧“取消”。
- 数字键盘三列不再撑满屏幕，横向与纵向间距统一并整体居中。
- `app.js`、`index.html`、`sw.js` 已统一到 `v536`。

## v535

- 锁屏和密码页的 Wi‑Fi 状态图标已换成指定的 SVG。
- 已移除锁屏底部手电筒、相机及其圆形按钮，保留上滑提示和底部横条。
- `app.js`、`index.html`、`sw.js` 已统一到 `v535`。

## v534

- 手机锁屏的锁图标、日期和时间整体向上移动 `30px`。
- `app.js`、`index.html`、`sw.js` 已统一到 `v534`。

## v533

- 已移除手机桌面的“最近使用”展示卡；浏览器、备忘录和活动数据仍正常保存。
- `app.js`、`index.html`、`sw.js` 已统一到 `v533`。

## v532

- 已移除手机桌面上的 `Welcome back` 和角色名字区域，应用图标自然上移。
- `app.js`、`index.html`、`sw.js` 已统一到 `v532`。

## v531

- 手机锁屏时间数字已加粗，并拆分为固定的两位数字、垂直居中的冒号、两位数字，避免字体冒号位置偏低。
- `app.js`、`index.html`、`sw.js` 已统一到 `v531`。

## v530

- `ta 的手机` 锁屏已改为原生 iPhone 风格：先显示时间锁屏，轻点或上滑后进入四位数字密码盘。
- 已移除锁屏上的头像、名字、输入框、初始密码提示和“忘记密码”卡片。
- 保留现有渐变壁纸；密码仍真实校验，初始密码仍为 `2580`，可在手机设置中修改。
- `app.js`、`index.html`、`sw.js` 已统一到 `v530`。

## v529

- Home 新增「ta的手机」入口，进入后先显示 Apple 原生质感的锁屏；初始四位密码为 `2580`，支持在手机设置里更改和安全重置。
- 手机桌面首批包含浏览器、备忘录和设置；浏览器保存真实搜索摘要与来源，备忘录保存 AI 自己留下的内容。
- AI 会在页面可运行时按低频计划自行决定浏览、写备忘录或什么都不做，不需要用户下命令；浏览器没有真实联网能力时不会伪造记录。
- 手机状态进入小屋存档；普通聊天只在话题相关时带入少量手机记录。
- `app.js`、`index.html`、`sw.js` 已统一到 `v529`。

## v519

- 新增可选主动消息与 Web Push：当最后一条消息来自 AI、用户长时间未回复且不在免打扰时段，才交给模型判断是否自然补发。
- 支持第一次等待、再次追问、最多追问、每日上限与免打扰时间；默认最多 2 轮、每天 2 条。
- 用户一回复会立即清空待发送追问；生成消息期间若聊天或配置发生变化，本次发送自动作废。
- 定时任务加入设备锁和状态版本校验，避免并发重复发送。
- 服务端加密保存主动消息所需的模型 API Key，浏览器通知使用 VAPID，待取消息仍写回本地聊天记录。
- 主动消息依赖 Upstash Redis、VAPID 环境变量和外部定时请求，完整配置见 `README.md` 与 `.env.example`。

## v515

- Moment 一级评论只显示评论者名字；只有实际回复用户评论时才显示“回复你”。
- 评论新增明确的回复对象字段，并兼容旧评论与旧演示数据。

## v514

- 私聊头像进入资料页时增加轻微右侧推进与淡入动效，返回聊天页时反向过渡。
- 遵循系统“减少动态效果”设置。

## v513

- 私聊中 AI 侧最长气泡限制在双方头像之间的共同内容轨道内，右边缘与用户消息对齐。

## v512

- 私聊分条符号由空格改为 `/`，普通空格不再拆分消息。
- `http://` 与 `https://` 链接中的斜杠不会触发消息分条。

给下一个 Codex 接手用。

## v572

- 新增原生 Node 后端的文件存储回退：未配置 Upstash Redis 时，主动消息和手机状态保存到服务器 `.data/`。
- 新增 `/api/phone` 与 `/api/phone-tick`，页面会同步手机状态，服务器后台每 5 分钟判断一次 AI 是否自行使用手机。
- 后台 AI 可真实写备忘录、添加/完成待办、浏览（已配置网页搜索时）和在允许时更换手机密码；结果会在下次打开手机时同步回来。
- Node 服务器自动启动手机后台任务和主动消息后台任务，并自动生成持久化 VAPID 密钥；主动消息推送仍需 HTTPS。
- `app.js`、`index.html`、`sw.js` 已统一到 `v572`。

## v573

- 后台手机自主使用调整为每天北京时间 09:00–24:00、每 2 小时检查一次；仍由 AI 自己判断是否操作。
- 主动消息调整为每天北京时间 09:00–24:00、每 90 分钟检查一次；检查只触发 AI 判断，不代表固定发送消息。
- `app.js`、`index.html`、`sw.js` 已统一到 `v573`。

## v574

- 修复手机后台同步可能用空的本地状态覆盖服务器记录的问题。
- 页面首次同步时优先取回服务器已有的备忘录、待办、浏览记录、活动和密码状态；服务器同步也会合并两边已有记录，避免更新后清空手机内容。
- `app.js`、`index.html`、`sw.js` 已统一到 `v574`。

## v584

- 手机后台备忘录判断改用与主动消息相同的对话上下文：最近 12 条对话、人设核心和常驻记忆。
- 手机后台检查改为每天北京时间 09:00–24:00、每 1 小时检查一次；AI 仍自行判断是否操作。
- `app.js`、`sw.js`、`api/phone-tick.js`、`server.js` 已统一到 `v584`。

## v585

- 接入 `image/` 文件夹里新增的 9 个表情包：不要这么对我呀、你咋这样、全都是坏人、小狐得志、我一直在哭、我知道了、我就这样萌萌、我想你我想你我想你、等待亲亲。
- `app.js`、`index.html`、`sw.js` 已统一到 `v585`。

## v586

- 统一压缩 `image/*.jpg` 表情包至最长边 512px、JPEG 质量 80，减少手机端资源体积；文件名与功能引用不变。
- `app.js`、`index.html`、`sw.js` 已统一到 `v586`，用于刷新缓存。

## v587

- 修复「人呢」表情包显示黑方块：缩略图缓存升级为 `v2`，避免旧版错误缓存阻止图片重新生成。
- `app.js`、`index.html`、`sw.js` 已统一到 `v587`。

## v588

- 简化主动消息设置页面：隐藏旧的等待时间、追问次数和免打扰输入，改为展示后台固定策略说明。
- 主动消息统一由后台按北京时间 09:00–24:00、每 90 分钟检查一次，并由 AI 判断是否发送；推送连接改为可选。
- 前端同步给后台的主动消息策略固定为适度低频（首次等待 4 小时、再次等待 4 小时、每天最多 2 条），避免旧页面设置造成混淆。
- `app.js`、`index.html`、`styles.css`、`sw.js` 已统一到 `v588`；后端程序无需重启。

## v589

- 主动消息页面进一步简化：移除后台策略说明卡片与手机推送入口，只保留主动消息开关。
- 主动消息仍按既定后台逻辑运行，打开网页时读取历史主动消息。
- `app.js`、`index.html`、`styles.css`、`sw.js` 已统一到 `v589`；后端程序无需重启。

## v590

- 修复手机自主使用的离线补记绕过北京时间 09:00–24:00 时间窗的问题，不再在凌晨生成备忘录或待办。
- 明确区分“页面离线”与“用户出门”等现实事件；后台与前端均禁止在最近对话没有依据时记录用户的出门、睡醒、天气体感等经历。
- 对自主备忘录增加事实依据保护，检测到无聊天依据的用户经历描述时直接放弃本次记录。
- `app.js`、`index.html`、`styles.css`、`sw.js` 已统一到 `v590`；后端程序需要重启后才会加载新的 `api/phone-tick.js`。

## v591

- 手机备忘录增加逐条“删除”按钮，删除前会确认，并同步更新本地存档和后端手机状态；使用删除标记避免后端合并逻辑把已删记录重新拉回来。
- `app.js`、`index.html`、`styles.css`、`sw.js`、`api/phone.js` 已统一到 `v591`；更新后端文件后需要重启后端程序。

## v592

- 修复手机后台写备忘录时引用未定义对话变量导致动作静默失败的问题；AI 决定写入后现在会正常保存。
- `app.js`、`index.html`、`styles.css`、`sw.js` 已统一到 `v592`；`api/phone-tick.js` 同步更新。

## v593

- 修复手机页面在后端暂时不可用时只尝试一次、之后一直不再同步的问题。
- 后端恢复后前端会自动重试连接；切回手机页面时也会立即重试，不需要重新填写 API 或重新添加 PWA。
- `app.js`、`index.html`、`sw.js` 已统一到 `v593`。

## v594

- 保留 Vercel 作为 PWA 前端，同时把 Vercel 的 `/api/phone` 与 `/api/proactive` 请求透明转发到腾讯云 Node 后端。
- 手机无需更换 PWA 地址或重新填写 API；Vercel 前端更新后，手机状态和主动消息记录会进入腾讯后端的持久化存储。
- `app.js`、`index.html`、`sw.js` 已统一到 `v594`。

## v595

- 聊天工具参数在发送前会移除部分 Gemini/第三方兼容接口不支持的 JSON Schema 限制字段。
- 如果上游仍返回工具定义不兼容错误，聊天会自动移除工具并重试一次，避免私聊完全不回复；动态等不带工具的请求不受影响。
- `app.js`、`index.html`、`sw.js` 已统一到 `v595`。

## v596（腾讯后端手机任务修复）

- 手机后台调用 AI 的单次等待时间由 25 秒延长为 55 秒。
- 网络错误、超时、限流或服务端错误会自动重试一次；鉴权、参数等明确的客户端错误不会盲目重试。
- 每次失败会把次数和具体原因写入腾讯云服务日志，设备处理失败也会留下可定位的日志。
- 手机后台任务锁延长到 180 秒，覆盖两次最慢请求，避免重试期间重复写入。
- 本次只修改 `api/phone-tick.js`；Vercel 前端仍为 `v595`，无需重新安装 PWA 或重新填写 API。腾讯后端替换文件后需要重启 `little-room` 服务。

## 项目位置

当前项目根目录：

```text
/Users/antongxu/Documents/little-room-v189-handoff
```

这是一个静态 HTML/CSS/JS PWA，小屋 App。用户通常通过 zip 上传到 GitHub/Vercel 更新。

## 当前版本

当前版本是 `v592`。

版本号需要保持同步：

- `app.js`: `APP_VERSION = "592"`
- `app.js`: service worker register `./sw.js?v=${APP_VERSION}`
- `index.html`: `styles.css?v=592`
- `index.html`: `app.js?v=592`
- `sw.js`: `CACHE_NAME = "little-room-v592"`
- `sw.js`: assets `styles.css?v=592` / `app.js?v=592` / MiSans 多字重字体 / 私聊输入框 SVG 图标

## v511 Notion 页面准确匹配

- 页面名称匹配会忽略 emoji、空格和不同样式的短横线，`To-Do List` 可准确命中 `🍧 To‑Do List`，不再同时读取多个相似页面。
- Notion 页面读取深度与内容上限提高，长页面不会过早停在较旧的周记录。

## v510 Notion 按名称搜索

- Notion 页面链接不再是调用前提；只要 Token 有权访问，用户说出页面名称即可搜索并读取。
- 常用页面配置只用于提高名称匹配率，不填写也不影响按名称查找。
- 情绪交流与普通闲聊的拦截仍保留，避免 Notion 无关触发。

## v509 新增表情包

- 新增 `乖宝宝`、`醒醒`、`我去你的`、`你在干嘛` 四张表情包。
- 已同步到私聊表情面板、AI 可选表情名称和 PWA 离线资源。

## v508 前端模拟预览

- URL 加 `demo=1` 即进入独立的前端模拟模式，不读取或写入真实小屋存档。
- 自动提供聊天、Moment、Diary、Journal、Memory、Todo 和资料页示例数据，无需填写 API。
- 私聊、发布动态、写/刷新 Diary、小便签和 Journal 等生成入口会返回本地模拟内容，不产生 API 消耗。
- 模拟模式不注册 Service Worker，也不启动天气、Journal、音乐等后台任务，避免影响正式版本。

## v498 资料页 Moment 行收敛

- 资料页 Moment 入口最小高度从 `5.5rem` 收敛到 `3.75rem`，上下内边距从 `0.85rem` 收敛到 `0.55rem`。
- 无图片预览时右侧辅助文字改为 `moment`。

## v497 Diary 刷新修复

- Diary 刷新不再把旧正文重复塞入系统上下文，只把旧文与当时聊天作为事实边界。
- 明确要求重新组织开头、结构、句式与措辞，避免刷新后几乎原样返回。
- 若模型第一次完整返回旧文，会自动重试一次；日期和写完后的新聊天不会混入。

如果继续修改，版本号递增 1，并同步以上位置。

## v496 私聊头像外侧留白

- 私聊记录区左右外扩改为 `2px`，在带 `1px` 外框的手机布局中，双方头像到屏幕边缘均为 `15px`。
- 头像到消息气泡的间距仍保持约 `6.7px`。

## v495 私聊头像与单行气泡等高

- 私聊双方头像由 `35px` 放大为 `2.71rem`，约 `43.36px`。
- 当前单行消息气泡实测约 `43.34px`，头像与其视觉等高。

## v494 移除表情包语境索引

- 删除每张表情包附带的情绪与使用语境标签。
- 私聊提示只向模型提供可用表情包名称，由模型结合当前聊天自行决定是否发送及选择哪张。
- 不增加固定概率、冷却轮数或前端拦截。

## v493 私聊头像边距对齐

- 聊天记录区域左右扩展 `calc(0.58rem + 1px)`，抵消页面外框后，头像到屏幕边缘与头像到气泡的间距均为 `0.42rem`。
- 左右双方消息保持对称，输入框位置不变。

## v491 底栏与顶栏统一

- 底栏背景改为与顶栏一致的 `#f7f7f7`。
- 去掉底栏顶部的分割线与毛玻璃效果。

## v490 私聊气泡圆角统一

- 私聊普通消息、语音与通话消息气泡统一使用与头像相同的 `6px` 圆角。
- 工具状态、动作描述和表情包继续使用各自原有样式。

## v489 私聊双方方形头像

- 私聊中的 AI 与用户头像统一为 `6px` 小圆角正方形。
- 设置页头像预览仍保持原样。

## v488 私聊 AI 方形头像

- 私聊消息左侧的 AI 头像改为 `6px` 小圆角正方形。
- 用户头像和设置页头像预览继续保持原样。

## v487 资料页头像底色

- 资料页未上传头像时使用浅灰绿色底色，与白色页面形成清晰边界。
- 仅影响资料页头像，不改变私聊和 Moment 头像。

## v486 Moment 方形头像

- Moment 动态列表中的头像保持原尺寸，改为 `6px` 小圆角正方形。
- 私聊头像和资料页头像不受影响。

## v485 资料页紧凑布局

- 删除资料区重复展示的大号显示名，只保留名字和备注。
- 去掉资料区固定高度，缩小上下留白，并让头像与两行资料自然居中对齐。

## v484 私聊备注名

- 联系人资料中的备注只作为用户本地可见的显示名，不进入人设、聊天 prompt、Journal 或 Memory。
- 填写备注后，私聊左上角和个人 Moment 页标题优先展示备注；清空备注后恢复原名。

## v483 联系人资料布局

- 资料页改为头像在左、名字与备注在右的信息布局，不再使用居中头像和表格卡片。
- Moment 入口独立成整行，并优先展示对方最近三张动态图片；没有图片时显示动态数量。
- 保留备注即时编辑、个人 Moment 跳转和原有黑白视觉风格。

## v482 个人 Moment 页面

- 联系人资料里的 Moment 入口不再跳到公共 Moment，而是进入独立的个人动态页。
- 个人动态页只展示对方发布的 Moment，不包含用户自己的动态，也不显示发布和清空入口。
- 点返回会先回到联系人资料，再返回私聊；点赞、评论、收藏和查看大图继续复用原有能力。

## v481 联系人资料二级页

- 点击私聊头像不再弹出浮层，改为直接进入联系人资料二级页面。
- 顶栏返回按钮回到私聊，底部保持聊天 Tab 选中；名字、备注和 Moment 入口保持不变。

## v480 联系人资料卡

- 私聊里点击对方头像会打开联系人资料卡，不影响气泡原本的操作入口。
- 资料卡显示头像、名字和可编辑备注；备注即时保存在本地，但不会擅自替换私聊顶栏名称。
- 资料卡提供 Moment 入口，可直接进入现有动态页面并显示对方动态数量。

## v479 Journal 卡片折叠

- 最近 Journal 卡片改为与 Diary 相同的折叠结构，默认只显示日期、自动整理状态和操作按钮。
- 点击卡片标题区域展开或收起时间线；点击“编辑”会自动展开卡片并显示时间、描述字段。
- 更新、编辑、删除和重试按钮不会误触卡片折叠。

## v478 时间线分栏编辑

- 最近 Journal 的“编辑”不再显示旧式整段正文，而是按时间段逐条显示“时间 + 描述”两个字段。
- 时间字段支持单点时间 `14:24` 和范围时间 `14:00–16:00`；描述修改后仍会写回当天 Journal，并等待长期 Memory 重新同步。
- “更新”继续只整理上次总结后的新增聊天，不会因为手动编辑而重写旧时间段。
- 编辑多个字段时只在收起编辑器后触发一次长期 Memory 同步，避免重复 API 调用。

## v476 去电自动接通

- 附件面板里的通话入口改为真正的去电流程：先显示「等待对方接通」，短暂停留后由对方自动接通并进入通话。
- 对方来电仍保留用户手动接听/拒绝；通话状态新增 `incoming/outgoing` 方向，AI 可正确感知。
- 去电等待期间可取消拨号，不再永久卡在等待接通。

## v474 Journal 增量更新

- Journal「更新」只读取上一轮总结完成后新增的聊天，不再把旧时间线交给模型重写。
- 旧时间段原样保留，新时间段由程序追加并按时间排序；没有新聊天时不调用 API。
- 自动分批整理同样改为增量追加，避免同一天内容被反复总结后越来越细。

## v473 手机强制换版

- 「检查更新」会注销旧 Service Worker、清空小屋缓存，并用一次性 `fresh` 地址重新载入，避免 Safari 继续运行旧版 `app.js`。
- 核心 JS/CSS 联网时改为网络优先且绕过 HTTP 缓存，离线时才回退到 PWA 缓存。

## v472 Journal 返回格式容错

- Journal 不再因为模型多输出代码块、使用数组、尾逗号或改用 `timeline/items` 字段而直接更新失败。
- 支持从普通的“开始时间-结束时间 + 总结”文本中恢复时间段。
- 无法恢复的返回会保留原 Journal，不再暴露技术格式错误。

## v471 Journal 时间段总结

- Journal 从分钟级单点事件改为 `开始时间–结束时间 + 时间段总结`，每天通常只保留 2–5 个主要阶段。
- 相邻、相关聊天会合并，不再生成逐句流水账；当时曾允许无新增聊天时重排旧记录，该行为已由 v474 的增量更新替代。

## v470 Journal 完整日分批整理

- 修复 Journal 每天超过 120 条消息后只整理第一批、时间线提前截断的问题。
- 同一北京时间自然日会按批次连续整理到最后一条聊天；所有批次完成后才同步长期 Memory，避免半截时间线提前沉淀。
- 手动「更新」同样会连续合并该日所有尚未处理的聊天。

## v468 聊天菜单自适应宽度

- 私聊右上角下拉菜单改为根据最长选项自动确定宽度，不再因固定宽度显得偏斜。

## v467 等待接通动效

- 小型来电浮窗的「等待接通」后增加三个依次跳动的圆点，作为轻量 loading 反馈。

## v466 小型来电浮窗

- 对方发起通话时，私聊页顶部先出现小型「等待接通」浮窗，不会直接打开完整通话页。
- 浮窗内可以接听或拒绝；接听后才进入原有完整通话界面，`ringing` / `active` 状态继续同步给 AI。

## v465 等待接通状态

- 从 `+` 面板发起通话后先进入「等待接通」，展示接听与拒绝按钮。
- 用户接听后才进入「通话中」并开始计时；AI prompt 会根据 `ringing` / `active` 获取对应真实状态。

## v464 通话入口迁入附件面板

- 私聊顶栏不再显示电话按钮。
- `+` 面板新增「通话」入口，并继续复用原有通话状态与记录逻辑。

## v463 附件面板贴合底栏

- 附件面板展开时取消输入区原有的底部安全留白，使浅灰面板直接衔接底部导航。

## v462 私聊添加面板

- 私聊输入框中的 `+` 改为展开式附件面板，不再使用遮罩或悬浮底部抽屉。
- 面板作为输入区的一部分把聊天内容向上顶，目前提供一个微信式「照片」入口。

## v461 私聊添加内容抽屉

- 私聊输入框中的图片按钮改为“+”。
- 点击后从底部拉起操作抽屉，当前提供“上传图片”选项。
- 选择上传图片后继续使用原有的图片预览、可选文字和发送流程。

## v460 私聊顶栏更多菜单

- 私聊顶栏保留电话按钮，并把 API 链接检测与清空聊天记录收进右上角省略号菜单。
- 下拉菜单支持点击空白处、切换页面和按 Esc 自动关闭。
- 两项操作继续复用原有连接检测和清空确认逻辑。

## v459 移除悄悄话

- 删除 Profile 中的悄悄话入口及二级页面。
- 删除悄悄话的新增、展示、删除和本地状态逻辑。
- 私聊 prompt 不再携带悄悄话内容，避免隐性影响回复并减少输入 token。

## v458 Journal 北京时间自然日时间线

- Journal 固定按 `Asia/Shanghai` 自然日归档，每天 `00:00` 换日，零点后的消息属于新一天。
- App 在午夜保持打开时自动整理刚结束的一天；关闭期间错过的整理，会在下次打开时按最早未整理日期补做。
- 每日 Journal 使用结构化时间线，模型只返回 `HH:mm + 事件`，日期和排序由程序控制，避免模型自行生成错乱时间。
- 同一天的新聊天会合并进当日时间线，不再重复创建多张 Journal；Memory 自动沉淀仍在 Journal 成功后执行。
- 模型返回格式异常时不推进已整理游标，原聊天会完整保留等待重试。
- 旧 Journal 会按实际覆盖截止时间迁移到对应的北京时间自然日，不破坏原有 Memory 关联。

## v455 Journal 单一时间轴

- Journal 标题时间改为由程序根据本次实际纳入的最后一条聊天确定，不再让模型自由生成日期时间。
- 模型只返回结构化的摘要正文；创建与更新时统一合成为“记录截止时间 + 正文”，避免创建时间、事件时间和刷新时间互相冲突。
- 最近 Journal、相关召回、Memory 自动沉淀、列表排序和 15 条清理统一按 Journal 的覆盖截止时间与更新时间排序，不再使用旧 `createdAt` 冒充最新记录。
- Journal 卡片显示覆盖截止时间；24 小时自动整理仍单独按最后整理/尝试时间计算，二者互不覆盖。
- 所有 Journal 显示与上下文时间固定使用东八区，避免手机所在时区改变记录日期。

## v454 Notion 工具语义闸门

- Notion 工具调用改为结构化意图，模型必须同时提交页面、最新用户原话依据、调用类型和必要原因。
- 前端执行前会验证依据确实来自最新消息，并确认目标是已配置页面或用户本轮提供的 Notion 链接。
- 仅放行用户明确要求读取页面，或用户正在询问存放于页面里的计划、待办、日程、笔记、文档、复盘等私人资料。
- 道歉、安慰、争论、情绪交流和普通闲聊中的误调用会被静默驳回，不显示工具消息，并要求模型按真实聊天语义继续回复。

## v453 Memory 时间归因与事实证据

- 长期 Memory、常驻记忆、重要经历和当前对话摘要现在带有明确的来源与时间边界，避免模型把无日期的身体状况、偏好和日常背景拼成“昨晚发生过”的事件。
- 固定系统规则要求：只有用户角色的最近聊天原话，或正文写有明确日期的 Journal，才能证明具体时间发生过某件事。
- AI 自己先前的回复、推测和提醒不再能作为用户事实的证据；用户质疑记忆时，模型必须重新核对来源，没有直接依据就承认记错，不能继续坚持或虚构“聊天记录”。

## v452 跨模型固定对话边界

- 小屋在每次私聊的稳定系统提示中固定加入对话边界，不依赖用户当前选择 Haiku、Sonnet 或其他模型。
- 默认禁止模型自行补写动作、神态、语气、环境、心理、人物和剧情；只有用户明确要求或正在直接发起动作互动时，才允许一处相关的简短动作。
- 恋人关系只影响称呼、关心方式和回应亲疏，不再被模型理解成需要持续写小说式亲密情节。
- 规则位于人设、对话风格和工具说明之后，靠近最终输出协议，降低换模型后的执行漂移；同时仍属于可缓存的稳定 prompt。

## v451 修复 iPhone Safari 表情面板重叠

- 不再依赖 Safari 容易错误计算的 Grid `aspect-ratio`，改用百分比占位生成稳定正方形网格。
- 图片绝对定位在各自网格内，不参与行高计算，避免跨行覆盖。
- 新 Service Worker 安装后立即激活并接管页面，减少手机继续使用旧样式的情况。

## v450 修复手机端表情面板重叠

- 表情选项使用稳定的正方形定位容器，避免手机 Safari 将图片原始尺寸计入 Grid 行高。
- 图片固定裁切在各自网格内，继续保持每行 4 个、面板内纵向滚动。

## v449 新增表情包

- 私聊表情面板新增 8 张 `image` 文件夹中的表情包。
- 新表情同步加入 AI 可选表情名称清单，可由用户发送，也可由 AI 按语境选择。
- 新素材加入 PWA 预缓存，更新后首次安装即可离线读取。

## 打包方式

修改后先检查：

```bash
node --check app.js
node --check api/chat.js
node --check api/weather.js
```

打包命令示例：

```bash
rm -f little-room-v444.zip && zip -r little-room-v444.zip index.html styles.css app.js sw.js manifest.webmanifest vercel.json README.md HANDOFF.md api icons image YunFengHanChanTi -x '*.DS_Store' '*/.DS_Store' 'image/1.jpg'
```

## v448 Journal 与 Memory 自动整理链路

- Memory 候选现在明确区分“可更新自动记忆”和“只读用户确认记忆”；只读项不再向模型暴露可误用的 `target_id`。
- 模型返回无效或受保护的更新目标时，本地会安全匹配已有自动记忆、跳过重复的确认记忆，或降级新建，不再让 Journal 卡在同一个目标错误里。
- 一次自动沉淀改为完整校验后提交；处理中任一项异常都会恢复本次修改前的 Memory，避免只写入一半。
- Memory 自动整理失败采用 30 分钟起的退避重试，并提供手动“重试”按钮；打开页面不再反复调用 API。
- Journal 生成失败采用 10 分钟起的退避重试，避免每发一条私聊都再次触发失败请求。

## v447 Journal 刷新时间与自动整理计时

- Journal 的聊天覆盖边界与 24 小时自动整理计时已拆开：`sourceEndAt` 只记录聊天整理到哪里，实际生成或刷新时间单独作为下一轮计时起点。
- 凌晨刷新包含晚上聊天的 Journal 后，下一次自动整理会从凌晨刷新完成时开始计算，不再错误地从晚上聊天时间开始。
- 最近 Journal 卡片显示最后更新时间；正文中的日期时间仍表示实际事件时间。

## v444 高德路线执行校验

- 高德工具参数新增最新消息原文依据，禁止从 Memory、Journal 或历史聊天里猜目的地。
- 前端在真正打开高德前再次核对：本句必须明确提出出行请求，且目的地必须出现在本句中。
- “打电话陪我”“我到家了”等通话或状态表达会静默拦截，不显示错误的高德工具记录。

## v443 私聊结构化回复

- 私聊改为结构化返回正文、表情和语音，避免 Haiku 把时间戳或“发送了表情包”这类内部上下文记录当成正常消息复述。
- 历史表情上下文改为专用记录格式；旧版 `[sticker:名称]` / `[voice:内容]` 仍保留兼容。
- 兼容模型分段返回的消息内容，并把误复述的完整表情记录恢复为真正的表情消息。

## v442 Home 便签落款

- 便签生成改为结构化称呼、正文和署名，日期由前端统一补齐，不再依赖不同模型遵守三行格式。
- 兼容 Haiku 把署名与日期拆成两行的旧便签，渲染时会自动合并并固定右对齐。

## v441 Journal 整理修复

- Journal 生成与长期 Memory 自动沉淀改为独立状态：Journal 成功后立即保存，后续沉淀静默执行；沉淀失败只记录在自动整理状态里，不再把 Journal 显示成整理失败。
- 兼容 API 返回分段文本内容，避免部分模型返回内容数组时被误判为没有消息。

## v440 收藏语音播放

- 收藏夹里的语音消息现在可以直接点击播放，并沿用私聊语音的波形播放动效。
- 语音仍在最近聊天中时，收藏夹直接引用原音频；原消息被删除、清空或超过 2000 条淘汰前，音频才转存进收藏，避免无意义地重复占用存储。

## v439 Moment 大图预览

- 点击动态卡片中的图片缩略图可打开全屏大图；点击背景、关闭按钮或按 Esc 可退出。
- 大图层复用原图片数据并在关闭时清空临时图片引用，不额外写入存档，也不影响 AI 识图。

## v438 Moment 卡片图片缩略图

- 动态卡片里的图片展示改为 100px 正方形缩略图；只调整前端样式，不影响保存图片或 AI 识图。

## v437 Moment 图片预览尺寸

- 发布动态的正方形图片预览由 88px 调整为 100px。

## v436 Moment 图片预览尺寸修正

- 发布动态的图片预览外层和图片本身都固定为 88px 正方形，避免浏览器按原图比例重新撑开。

## v435 Moment 图片缩略图

- 发布动态时的图片预览收为 88px 正方形缩略图，横图和竖图都会居中裁切显示。

## v434 Moment 自适应输入区

- Moment 文字区根据输入行数自动增高，图片按钮始终紧跟文字区；发布按钮保持在抽屉底部。

## v433 Moment 无框输入区

- Moment 发布抽屉的文字输入区去掉描边、独立底色和缩放手柄，直接融入抽屉。

## v431 Moment 发布抽屉高度

- Moment 发布抽屉高度调整为屏幕约三分之二，文本框自动填充剩余空间。

## v430 Moment 发布抽屉

- Moment 发布界面改为贴底抽屉，从屏幕底部滑入，并与动态操作菜单保持一致。

## v429 Moment 图片动态

- 用户发布 Moment 时可选择一张图片，也可同时填写文字；图片会压缩后随动态保存。
- 图片动态由视觉模型读取后再生成评论；动态收藏快照也会保留图片。

## v428 Moment 操作菜单

- Moment 删除入口从底部操作栏迁移到卡片右上角的 ellipsis icon。
- 点击三点后从屏幕底边拉起全宽操作层，不再悬浮；红色“删除动态”是唯一操作，点击遮罩或按 Escape 可关闭。
- 底部操作栏现在只保留点赞、评论和收藏。

## v423 收藏功能

- Moment 操作栏新增星形收藏按钮，收藏内容以独立快照写入 `favoriteMessages`。
- 收藏的 Moment 会在 Profile 的“收藏消息”页展示作者、原发布时间和正文；即使原动态被删除，收藏快照仍会保留。
- 私聊普通消息和语音消息的收藏 icon 已统一为同一款 star icon。
- 从收藏页取消收藏时，会同步重绘私聊和 Moment 的收藏状态。

## PWA 图标

- iPhone 加到主屏幕优先看 `apple-touch-icon`，不要只放 `svg`。
- 当前已改成 PNG：
  - `icons/apple-touch-icon.png`（180x180）
  - `icons/icon-192.png`
  - `icons/icon-512.png`
- `index.html` 和 `manifest.webmanifest` 都已经指向 PNG。

注意：

- 不要把旧版本文件夹、旧 zip、`.DS_Store` 打进去。
- 不要打包 `image/1.jpg`，聊天背景已经不用这张图。
- 需要保留 `README.md`，之前用户发现有 README 时 GitHub/Vercel 更新更正常。

## 重要功能现状

### 普通聊天上下文

普通聊天发送给模型：

- system prompt
- 最近 10 到 12 轮聊天
- 最近 2 条 Journal
- Todo 一句摘要（仅在有未完成待办时）
- 共读一句摘要（仅在真实导入并且已有阅读进度时）
- 当前东八区时间
- 今日天气 summary
- Profile 里的核心人设、权威分层后的 Memory、对话风格参考
- 如果 API 页开启了“网页搜索”且 API 为 OpenRouter，模型会先通过 `search_web` 结构化工具判断用户是否明确需要联网；确认调用后，下一次请求才附加 OpenRouter `web` plugin，`max_results` 为 `1`。不再由前端扫描“搜一下”等关键词。

最近聊天的每条消息会带时间戳给模型看，例如：

```text
[今天 21:04] 我到家了
```

但 AI 回复里不应该显示时间戳。当前代码有两层处理：

- system prompt 告诉模型：方括号时间只是消息时间，不是回复格式。
- `stripReplyTimestamp()` 会清理 AI 回复开头误输出的时间戳。

### Journal

- 自动 Journal 间隔：距离上一次 Journal 满 24 小时后整理一次。
- Journal prompt 要求 30 到 50 字。
- Journal 自动整理禁止保留“今天 / 明天 / 昨天 / 今晚 / 明早 / 昨晚 / 当晚”等相对时间词，必须根据消息时间换成具体日期和星期，避免之后私聊把旧的“明天”误读成当前的明天。
- 私聊读取旧 Journal 时，也会提醒模型：Journal 里的相对时间词只能相对该 Journal 开头日期理解，优先看绝对日期。
- Journal 用于承接上下文，用户可以手动添加、编辑、删除。
- 最新一条 Journal 有“更新”按钮：会把该条之后的新聊天合并进去并重写原卡片，不需要删除后重新生成。
- Journal 条目保存 `sourceStartAt / sourceEndAt`，生成进度以实际纳入的最后一条消息为准；模型返回“无”时也会推进到本批结尾，避免反复扫描同一段聊天。
- v415 起 Journal 额外保存 `sourceType`：`chat / manual / book / legacy`。只有聊天来源会推进聊天整理游标，手动 Journal 和共读记录不会再跳过尚未整理的私聊。
- Journal 按最早未处理消息分批整理；达到单批上限时会把同一时间戳的一组气泡一起纳入，避免分条气泡被截断后漏记。模型返回“无”也会推进到本批结尾，避免重复扫描同一段。
- Journal 整理会纳入普通文字、语音文字、图片附言和通话记录；提醒时间或计划时间不等于事情已经发生。
- 普通聊天只读最近 2 条 Journal，控制 token。
- Journal 现在固定按日期取最近 2 条；如果当前消息命中过更旧 Journal，会另起“自动召回线索”，低于常驻记忆和长期 Memory。

### Memory 权威等级

- 常驻记忆：用户手动固定，每次私聊都会带，权威最高。
- 长期 Memory / 重要经历：每条都有 `priority`、`authority`、`injectMode`。
  - `authority`: `confirmed`（用户确认）/ `derived`（自动整理）/ `candidate`（线索）。
  - `injectMode`: `always`（每次带）/ `relevant`（相关时带）/ `archive`（只存档，不进 prompt）。
- 手动新增默认 `confirmed + relevant`。
- AI 从 Journal 自动沉淀出来默认 `derived + relevant`。
- 私聊时间判断以动态注入的当前东八区 `NOW` 为唯一实时时钟；提醒、作息、Journal 与 Memory 只能表示计划或历史，不能据此声称“现在快几点了”。
- 私聊 prompt 会把不同权威的记忆拆开说明，避免自动总结压过用户手写事实。
- v415 起相关召回结合关键词与中文字符相似度，不再只靠完整短语命中；例如“我想妈妈了”可以召回标题不同但内容相关的经历。
- `injectMode=always` 会完整兑现“每次带入”，不再受普通相关记忆条数限制；旧版大段 Memory 只迁移一次，用户删空结构化记忆后不会重新出现。
- Journal 与自动 Memory 通过来源 ID 保持同步：重写或编辑 Journal 会重新整理并清理旧的自动记忆；用户手动编辑过的 Memory 会升级为 `confirmed`，自动整理不能覆盖。
- 自动记忆更新必须命中真实且可更新的目标；目标无效时保留旧记忆等待重试。相似的用户确认记忆只读，自动整理不会再创建重复项。
- 当前对话摘要删除/重写相关聊天时会自动失效，清空聊天也会同步清空摘要；摘要只把用户明确说过或确认过的内容当成事实。

### 天气

- Home 页面有天气卡片。
- 用户配置和风天气 QWeather API Key / Host。
- 默认城市北京。
- 北京固定 locationId：`101010100`，北京不需要 GeoAPI；其他城市可能需要 GeoAPI。
- 自动更新逻辑：
  - 东八区每天 9:00 后才自动更新。
  - 当天成功更新过就不重复。
  - 如果失败，隔 1 小时允许自动重试。
  - 从后台回到前台也会检查。

### Notion 工具

- API 页工具调用里新增 `Notion` 二级页面。
- 配置项：
  - `Notion Token`：可填在本地页面；如果留空，则后端读取 Vercel 环境变量 `NOTION_API_KEY`。
  - `常用页面`：可选，每个页面用独立的“页面名称”和“页面链接”输入框填写，支持添加和删除。底层仍兼容旧版 `名称 | 链接` 数据并会自动拆分显示。
- 后端接口：`api/notion.js`。
  - 使用 Notion `/v1/search` 搜页面。
  - 使用 `/v1/blocks/{block_id}/children` 读取页面块内容。
  - 最多递归 3 层、约 90 个 blocks，并截断到约 6500 字符，避免 prompt 过长。
- 私聊把 `read_notion_page` 作为真正的函数工具交给模型，由模型决定是否调用：
  - 用户明确要求查看某个页面时必须调用。
  - 模型判断当前问题确实需要用户保存在 Notion 里的私人资料时，也可以主动调用。
  - 参数可以是页面名称、Notion 页面链接或 Page ID；常用页面用于提高名称读取的准确性，但不是必填。
- 模型发起工具调用后，前端才显示 `调用工具：Notion`，请求 `api/notion.js` 读取页面，再把结果作为标准 `tool` 消息交回模型继续回答。
- 删除了 `今天安排 / To-Do List` 等前端关键词触发，普通聊天不会因为命中某句话而自动读取 Notion。

### 图片识别

- 私聊支持上传图片。
- 图片弹窗支持同时填写一段可选消息；发送后前端显示为图片消息加文字消息，文字仍按空格拆分气泡，但给模型只算作同一轮“发图并说了什么”。
- 图片只识别一次，不保存原图。
- 存入聊天记录的文案是：`发送了一张图片`。
- 图片压缩后发给模型，减少 token/流量。

### 表情包

表情包在 `image/`：

- 不行.jpg
- 人呢.jpg
- 哭哭.jpg
- 哼.jpg
- 贴贴.jpg
- 道歉.jpg
- 一直粘着你.jpg
- 好想你.jpg
- 求婚.jpg
- 等你信息.jpg
- 累累的.jpg
- 老公.jpg

### MiniMax 语音

- API 页新增“MiniMax 语音”二级页面。
- 需要用户自己填写 MiniMax Key、模型、Voice ID、语速。
- MiniMax 语音二级页里有“保存语音设置”按钮：如果用户已经在 MiniMax 文档里克隆成功，只需要把上方 Voice ID 填成已克隆的 voice_id，再点这个按钮保存，不需要点“克隆声音”。
- 后端接口：`api/tts.js`，调用 MiniMax `https://api.minimaxi.com/v1/t2a_v2`，使用非流式短语音，`output_format: "hex"`，返回前端可播放的 mp3 data URL。
- “MiniMax 语音”页新增声音克隆：
  - 上方 Voice ID 是“当前 TTS 使用的声音”；下方“新 Voice ID”是要克隆出来的新声音名。
  - 用户可以选择 mp3 / m4a / wav，或填已有 `file_id`，再点击“克隆声音”。
  - 如果选择音频，前端把音频以 data URL 传给 `api/voice-clone.js`，后端先请求 `https://api.minimaxi.com/v1/files/upload` 上传复刻音频，再请求 `https://api.minimaxi.com/v1/voice_clone` 创建 voice_id。
  - 如果填已有 `file_id`，后端跳过上传，直接请求 `https://api.minimaxi.com/v1/voice_clone` 创建 voice_id。
  - 因为 Vercel 函数请求体有限制，前端限制音频尽量 3MB 内；建议 10 到 30 秒干净人声 m4a。
  - `vercel.json` 已给 `api/voice-clone.js` 配 `maxDuration: 60`，避免上传 + 克隆超时。
- 私聊触发：
  - 用户明确说“发语音 / 语音哄我 / 想听你声音”等，会生成一条语音。
  - AI 必须把真正要朗读的话放进独立字段 `[voice:要朗读的一句话]`，前端只用这个字段生成语音，不再从普通正文里猜哪句要转语音。
  - 用户明确要求语音时，普通文字气泡会被收掉，只保留语音气泡；prompt 要求模型不要写“我发语音了 / 声音传来 / 按下录音”这类说明或旁白。
  - 最近聊天上下文里的历史语音格式是 `[语音] 说话者：内容`，避免模型模仿“某某发送了一条语音：xxx”。`sanitizeVoiceText()` / `stripVoiceActionText()` 只保留为兜底清理，不承担核心选句逻辑。
  - 语音消息使用普通私聊气泡的 padding / 行高，只把内容做成 lucide `audio-lines` icon + 文案。注意语音消息 HTML 不要带模板换行空白，`.message-content` 对 voice 会覆盖成 `white-space: normal`，避免左侧出现假空白。
  - 语音气泡右侧有气泡外的 lucide `repeat` icon。点击后展开/收起语音原文，原文显示在下方较深的小气泡里，并用逐字出现的打字机动效。
  - 转文字 icon 右侧有独立收藏 icon，可直接收藏或取消收藏语音，不需要展开消息操作。
  - 语音气泡 icon 使用 lucide `audio-lines`。
  - 如果开启“主动语音”，会在 10 到 15 轮对话后，且语境适合（晚安、安慰、撒娇、想你等）才随机主动发一条。
  - 每次最多一条语音，语音只取 AI 回复里适合朗读的一句短句，不整段全转，控制费用。
- 被亲晕.jpg

用户可发送表情包。AI 可在回复最后加：

```text
[sticker:贴贴]
```

App 会拆成第二条表情包消息。

### Home

Home 页包含：

- 纪念日/头像卡片
- Diary 入口
- Book 占位入口
- 音乐播放器样式卡片
- 天气卡片
- 每日便签

每日便签使用本地字体：

```text
YunFengHanChanTi/YunFengHanChanTi-2.ttf
```

### Diary / Moment

- Diary 是 Home 的二级页面，默认折叠卡片。
- Diary 生成参考最近 60 条聊天和当天 Journal，不参考 Moment / 旧 Diary。
- Moment 支持用户发布、AI 评论、AI 发布动态。
- Moment 生成参考最近 15 条聊天消息，不参考最近 Moment。

### 通话

- Chat 顶栏有模拟通话入口。
- 支持来电弹窗、通话计时、缩小成可拖拽悬浮窗。
- 通话记录会写进聊天记录。
- 当前通话状态会写进 system prompt：
  - 来电中：告诉模型用户还没接。
  - 已接通：告诉模型用户已经接通、正在通话、已通话多久，并禁止继续催用户接电话或描写来电铃声。

## 用户偏好

- 用户希望先讨论时不要擅自改代码。
- 用户明确说“改吧 / 做吧 / 打包”时再执行。
- UI 细节很重要，小的颜色、字号、间距变化都要谨慎。
- 打包时要提供 zip 的本地路径。
- 说话可以温柔轻松，但不要太长。

## 最近一次关键修改

`v409` 新增网易云官方“一起听”实验接入：网易云设置页可单独保存 AI 账号 Cookie；用户明确要求一起听时，AI 账号创建房间、自动选择指定歌曲或用户每日推荐，并在私聊工具结果中显示可点击的“进入一起听”入口；房间会在小屋前台打开期间持续发送心跳，并在两小时内复用，避免重复建房。

`v408` 新增消息收藏与 Moment 点赞：点击私聊气泡后可收藏/取消收藏；收藏以独立轻量快照保存，不受最近 2000 条聊天上限、删除原消息或清空聊天影响，并可从 Profile 的“收藏消息”二级页集中查看。Moment 底部新增点赞按钮，点赞状态会随存档保存。

`v407` 为私聊语音消息增加播放动效：播放时语音 icon 的六条声波线会交错起伏，暂停、播放结束或失败后恢复静止；同一时间只播放一条语音，再次点击正在播放的气泡可以停止。

`v406` 将用户新加入 `image` 文件夹的 `得意`、`生气`、`老婆` 三张图片接入私聊表情列表和 AI 可用表情清单；新表情继续使用现有的四列滚动面板与本地缩略图缓存逻辑。

`v405` 将防重复和上下文承接扩展到全部结构化工具：网页搜索、高德路线、Notion、网易云都保存一份供下一轮理解的执行摘要；Claude 必须先读取最近工具结果，再按用户最新消息决定下一步，不能因为历史里出现过工具就自行重放；同一轮相同操作统一去重，网页搜索每轮只允许真正联网一次。

`v404` 修复网易云连续操作失忆与重复写入：工具执行结果会作为后续私聊上下文保留；Claude 可在同一轮连续完成“读取/搜索 -> 创建/加歌”等最多 5 个工具步骤；同一轮重复操作会被忽略；后端创建歌单和添加歌曲增加幂等检查，同名歌单或已有歌曲不会重复写入；工具完成后若模型返回空正文，会追加一次只负责说明结果的恢复请求。

`v403` 将 Claude Prompt caching 从默认 5 分钟调整为 1 小时 TTL，更适合用户断断续续持续聊天的使用方式；缓存范围仍只包含固定人设、风格、常驻记忆、固定规则与工具说明，动态状态不变。

`v402` 为 OpenRouter 上的 Claude 启用显式 Prompt caching：完整人设、对话风格、常驻记忆、固定规则与工具说明组成稳定缓存块；当前时间、通话状态、相关 Memory、Journal、Todo/共读/天气仍按每轮实时发送。OpenRouter 返回的当日输入、输出、缓存读写 token 与 cost 会累计保存在本地状态，便于后续增加用量界面。

`v401` 收紧结构化工具的语义调用边界：提到歌曲、歌单、地点或页面不再等同于要调用工具；讨论歌单名字、创意、评价、假设和普通闲聊会直接对话，只有确实需要账号数据或用户明确要求执行真实操作时才调用网易云等工具。

`v400` 修复网易云写操作的 `csrf is not defined`：后端现在会把已提交的 `__csrf` 继续传给创建歌单、歌单加减歌曲和红心等操作，不再在工具执行时丢失变量。

`v399` 将网页搜索和高德路线改为 Claude 结构化工具调用：移除前端搜索关键词与高德路线正则解析，Claude 根据整句话语义决定是否调用，并输出搜索查询或起点、终点、路线方式。网页搜索仍只在用户明确要求且 API 为 OpenRouter 时执行，每轮最多一次；高德只在明确路线意图时跳转。四类结构化工具共用一条简短权限规则，不再为每个工具堆叠长 system prompt。

`v398` 新增网易云音乐工具：API 页加入二级设置，可本地保存 `MUSIC_U` / `__csrf` 并测试连接；新增 `api/netease.js`，支持搜歌、查看歌单与歌曲、听歌记录、每日推荐，以及在用户明确要求时建歌单、加歌、移除歌曲和红心。私聊通过 `use_netease_music` 结构化工具调用，不使用前端关键词硬触发；工具不能控制手机 App 实际播放。

`v397` 移除了 Moment 评论输入框的 placeholder，展开后只保留空输入框与纸飞机按钮。

`v396` 将 Moment 评论区的“发送”文字按钮替换为与私聊相同的纸飞机 SVG，按钮保持约 `27px`，图标为 `14px`。

`v395` 收紧了 Moment 展开后的评论输入区：输入框从约 `33.4px` 降到约 `27px` 高，保持 `16px` 输入字号以避免 iPhone 聚焦缩放；发送按钮同步缩小。

`v394` 将 Moment 卡片底部评论和删除按钮的点击区域从 `36px` 收窄为 `24px`，图标继续保持 `14px`。

`v393` 将 Moment 卡片底部的评论和删除图标从 `20px` 缩小为 `14px`。

`v392` 将私聊顶栏状态文字从上提 `2px` 回调为上提 `1px`，让状态重新接近标题底部对齐，同时保留轻微的小字号视觉补偿。

`v391` 将私聊顶栏状态文字单独上提 `2px`，避免小字号在底边对齐时显得偏下；状态仍位于标题偏下区域，且不参与标题布局。

`v390` 修正了私聊状态文字对顶栏标题布局的影响：

- 状态文字改为相对标题行绝对定位，仍显示在标题右下侧。
- 状态不再参与标题行高度计算；私聊与 Moment 的标题行均为 `16px`，标题垂直坐标完全一致。
- 标题和图标继续直接共用 Moment 的全局尺寸。

`v389` 撤销了私聊顶栏的单独视觉补偿，私聊与 Moment 重新直接共用同一套顶栏样式：标题均为 `16px / 500`，图标外框均为 `16px`。

`v388` 曾对私聊标题和图标做单独视觉补偿，已在 `v389` 删除。

`v387` 修正了顶栏状态文字的对齐：标题与状态作为一组在顶栏中居中，状态改为与标题按文字基线对齐，不再与标题中心对齐。

`v386` 继续缩小顶栏内容：标题调整为 `16px`，状态文字调整为 `10px`，右侧图标调整为 `16px`；顶栏高度和垂直居中保持不变。

`v385` 修正了缩短后的顶栏垂直对齐：标题和状态改为在标题行内真正上下居中，顶栏整体高度保持不变。

`v384` 修正了顶栏上移方式：

- 撤销标题、状态和图标单独向上位移的做法。
- 顶栏整个区域高度减少 `10px`，聊天内容区随顶栏底边一起向上移动。
- 顶栏内部文字与图标仍按区域正常对齐。

`v383` 曾把顶栏内部元素单独向上移动 `10px`，已在 `v384` 撤销。

`v382` 把页面顶栏标题字重从浏览器默认的 `700` 调整为 `500`，其他顶栏样式保持不变。

`v381` 缩小了页面顶栏文字和图标：

- 顶栏标题从 `20px` 改为 `18px`。
- 状态文字保持 `11px`。
- 顶栏右侧图标统一从 `19px` 改为 `18px`。

`v380` 调整了私聊消息时间的展示频率：

- 同一方连续发送的多个气泡只在这一轮最后一个气泡下方显示一次时间。
- 动作描述和工具调用提示不单独算一轮，也不会截断同一方连续消息的时间归组。
- 时间仍保留在气泡下方原来的位置，样式不变。

`v379` 把私聊消息时间恢复到原来的位置：

- 时间重新放在消息气泡下方独立一行，恢复原有外边距和左右内边距。
- 保留与顶栏状态一致的时间颜色。
- `v378` 的用户消息按空格分气泡功能保持不变。

`v378` 新增了用户消息按空格分气泡的展示规则：

- 用户一次输入的内容会按普通空格、Tab 或全角空格拆成多个独立的我方消息气泡。
- 同一次发送拆出的气泡使用同一个 `displayGroupId`，时间一致。
- 构建 AI 最近聊天上下文时会按原顺序重新用空格合并，仍按一轮用户消息计算，避免上下文和 token 逻辑被前端展示拆分影响。
- 通话页发送时也会同步显示拆分后的我方消息。

`v377` 统一了气泡正文与时间的文字基线：

- 移除时间的人工垂直偏移，改为与正文共享基线对齐。
- 时间颜色、字号、格式和 `2px` 横向间距不变。

`v376` 继续微调气泡内时间的垂直位置：

- 时间改为相对正文基线下移 `1px`，位置介于基线对齐与文字底部对齐之间。
- 时间颜色、字号、格式和 `2px` 横向间距不变。

`v375` 微调了气泡内时间的垂直位置：

- 时间从整行盒子底部对齐改为正文文字底部对齐，避免位置过低。
- 时间颜色、字号、格式和 `2px` 横向间距不变。

`v374` 调整了气泡内时间的垂直对齐：

- 消息时间由文字基线对齐改为与正文最后一行底部对齐。
- 时间颜色、字号、格式和 `2px` 横向间距不变。

`v373` 调整了气泡内时间的颜色：

- 消息时间颜色改为与私聊顶栏状态文字一致的 `rgba(39, 35, 31, 0.48)`。
- 时间字号、位置和 `2px` 间距不变。

`v372` 微调了气泡正文与时间的距离：

- 气泡内正文末尾到时间的间距由约 `6px` 改为 `2px`。
- 时间的字体、颜色、字重和格式不变。

`v371` 微调了气泡内时间间距：

- 去掉正文末尾与时间标签之间由 HTML 模板换行产生的自然空格。
- 保留原有约 `6px` 的 CSS 间距，其他时间样式不变。

`v370` 把私聊消息时间移入气泡：

- 普通文字、图片提示、通话记录和语音消息的时间现在位于气泡内部，紧跟消息正文末尾。
- 时间字号、颜色、字重和格式保持原样。
- 表情包没有文字正文，时间仍贴在表情图片尾部显示。

`v369` 调整了私聊消息时间的位置：

- 时间从气泡下方独占一行改为放在消息气泡尾部，并与气泡底边对齐。
- 时间仍然位于气泡外，字号、颜色、字重和格式保持不变。
- 文字、语音、图片、表情和通话记录共用同一套尾部时间布局。

`v368` 明确了 API 温度设置的归属：

- 删除 `v366` / `v367` 对已保存温度的自动迁移与自动回滚。
- API 页面保存多少温度，后续就持续使用多少；代码不再擅自改动。
- `0.8` 只作为从未保存过 API 设置时的初始默认值。

`v367` 回滚了 `v366` 的人设稳定性调整：

- 删除 system prompt 末尾新增的“人物一致性最终校准”。
- 默认温度恢复为 `0.8`。
- 已经加载过 `v366` 且仍为迁移值 `0.7` 的设备会自动恢复到 `0.8`；用户手动设置的其他温度不受影响。

`v366` 加强了私聊的人设稳定性：

- 在 system prompt 末尾增加人物一致性最终校准，明确核心人设与对话风格负责身份、关系、性格和语气。
- Memory、Journal、天气和工具结果只作为事实上下文，不能把角色带成总结者、提醒助手或客服。
- 最近聊天中的 AI 历史回复只用于衔接事件，不再被视为新的风格范例；若历史回复跑偏，应回到核心设定。
- 默认温度由 `0.8` 调整为 `0.7`；旧默认值会自动迁移一次，之后仍允许用户手动修改。

`v365` 给本地聊天历史增加了容量保护：

- 小屋只保留最新 2000 条 `state.messages`，载入旧存档和每次保存前都会裁剪。
- 从 4802 条升级时会保留最新 2000 条，最旧部分不再写回手机。
- 初始化完成后会主动保存一次，让旧数据自动完成裁剪迁移。
- `localStorage` 写入失败时不再静默丢记录：会显示“手机存储空间不足”提示。
- 主存储因容量不足时会先清除可重建的表情包缩略图缓存，再重试一次。

`v364` 放宽了 Moment 的硬字数限制：

- AI 发布的 Moment 从强制 `8 到 16 字` 改成通常 `1 到 3 个短句、约 15 到 60 字`，自然完整优先。
- AI 评论用户 Moment 改成通常 `1 到 2 个短句、约 8 到 40 字`。
- AI 回复自己 Moment 下的评论改成通常 `1 到 2 个短句、约 8 到 50 字`。
- 主语关系和人格连续规则保持不变。

`v363` 优化了 Notion 常用页面的填写方式：

- 不再要求用户手写 `名称 | 链接` 格式。
- 每个页面显示两个独立输入框：页面名称、页面链接；右侧可删除，下方可继续添加。
- 保存时仍转成现有轻量数据格式，因此旧存档和已有配置可以无损迁移。
- Notion 自主工具与名称到链接的映射逻辑保持不变。

`v359` 收紧了高德路线的状态句误触发：

- `我到家了 / 刚到家 / 快到公司了 / 我回家了` 这类生活状态不再触发高德。
- 没有路线语境时，只允许非常像命令的 `去公司 / 回家 / 往学校` 触发别名路线。
- 带明确路线语境的句子仍然触发，例如 `打车到公司 / 导航去公司 / 从中关村到知春路怎么走`。
- 这次不是按单句黑名单处理，而是把“到达/回到状态”和“路线命令”拆开判断。

`v358` 简化优化了 Notion 多页面配置：

- Notion 设置页从单个“默认页面”改成“页面列表”。
- 用户可以一行填一个页面，格式 `名称 | 页面链接或 Page ID`。
- 私聊查询时会优先按页面名称匹配；问“今天安排 / 这周计划 / 待办”这类没有明确页面名的问题时，会读取页面列表前几个页面。
- 没有匹配到配置页面时，仍可回退到 Notion 搜索。

`v357` 新增 Notion 查询工具：

- API 页新增 `Notion` 二级页面，可开启工具、填写 Token、页面列表和默认搜索词。
- 新增后端 `api/notion.js`，避免直接从前端请求 Notion；支持 Vercel 环境变量 `NOTION_API_KEY`。
- 私聊里问日程、周计划、待办，或明确说查 Notion/页面/笔记/文档时，会调用 Notion 并把结果作为本轮上下文。
- Notion 内容做了块数量、递归层级和字符数限制，避免 token 暴涨。

`v356` 收紧了高德路线的 “从 A 到 B” 触发条件：

- “从 A 到 B” 不再天然等于路线工具，必须有路线语境，或起点/终点命中 `家 / 公司 / 学校` 这类高德别名，才会调用高德。
- “从小学到大学”这类经历/时间段表达不会再触发高德。
- “从公司回家 / 从家到公司 / 从公司到某地怎么走”仍然可以正常触发。

`v355` 重构了高德路线识别：

- 不再只靠关键词和零散正则直接触发高德。
- 先把用户消息解析成结构化路线意图：`from / to / mode / confidence / reason`，再决定是否调用工具。
- “去公司狐狐”会先把“狐狐”当作称呼尾巴剥离，再把“公司”按高德别名解析；不会再把“公司狐狐”当成地点。
- “从 A 到 B / 从 A 回 B”这类句子会优先走明确起终点；“打车到公司 / 导航去公司 / 回家”会走目的地意图。
- 没有明确目的地时不会调用高德，只让 AI 正常回复。

`v354` 重构了语音消息生成逻辑：

- AI 不再由前端从普通正文里猜哪一句要转语音。
- 私聊回复新增独立语音字段约定：`[voice:要朗读的一句话]`。
- 用户明确要求语音时，prompt 要求 AI 必须把朗读内容放进 `[voice:...]`；普通正文可以为空或只写很短承接。
- 主动语音只在本轮允许时才提示 AI 可选输出 `[voice:...]`，前端只读取这个字段生成语音。
- `sanitizeVoiceText()` / `stripVoiceActionText()` 仍保留为兜底清理，但不再承担“从正文挑语音”的核心职责。

`v353` 重构了通话状态和 AI 感知逻辑：

- 新增 `state.callSession`，用 `idle / ringing / active` 作为通话状态来源，不再只靠 UI 是否隐藏和关键词猜测。
- 用户明确要求打电话时，前端会先切到 `ringing` 并弹出来电，再请求 AI；AI prompt 会看到“电话已经发起、正在等待用户接听”。
- 用户接通后切到 `active`，prompt 明确告诉 AI 正在通话中，禁止问“接通了吗 / 听得见吗”。
- 页面刷新或检查更新时会把 active 通话写成“通话中断”；若极端情况下刷新没触发 pagehide，重新打开时也会自动收口旧通话状态。
- 过滤电话旁白仍保留为兜底，但核心不再依赖过滤，而是让 AI 先读真实通话状态。

`v352` 修了打电话后 AI 追问“接通了吗”的问题：

- 用户明确要求打电话时，临时 prompt 现在禁止“接通了吗 / 听得见吗”这类连接确认句。
- 前端新增 `sanitizeCallReplyContent()` 兜底过滤电话旁白、连接确认句，以及被拆出来的孤立括号。
- 这样通话 UI 已经接管“是否接通”的状态，私聊气泡里不会再显示这类反直觉句子。

`v351` 修了通话中关键词误触发来电的问题：

- `shouldTriggerCall()` 现在会先判断是否已经在通话 / 来电中，如果是，就不会再次弹来电。
- 用户消息和 AI 回复使用不同的触发规则；AI 回复里单纯提到“通话中 / 电话”不会再触发来电。
- AI 只有明确说“给你打电话 / 打给你 / 拨给你 / 接电话”等强意图时，才可能触发来电。

`v350` 调整了通话记录前端文案：

- 正常挂断显示为 `通话结束 03:12`。
- 刷新、检查更新或页面关闭导致的中断显示为 `通话中断 03:12`。
- 兼容旧的 `03:12（中断）` 和 `03:12` 记录，前端会按新文案展示。

`v349` 修了通话中刷新/检查更新导致通话记录丢失的问题：

- 通话记录原本只在正常点击挂断时写入聊天。
- 现在检查更新、页面刷新或离开页面时，如果仍在通话中，会先写入一条通话记录。
- 刷新/更新导致的记录会显示为 `时长（中断）`，正常挂断仍显示普通时长。

`v348` 优化了表情包历史渲染：

- 新增单独的 `little-room-sticker-thumbs-v1` 缩略图缓存。
- 表情包第一次加载后会压成最大 220px 的小图，后续聊天历史和表情面板优先使用缩略图。
- 缩略图不写进聊天记录和导出存档，避免存档变大；缓存不可用时会自动回退到原始 `image/*.jpg`。

`v347` 修了私聊页上滑时被突然拉回底部的问题：

- `renderMessages()` 不再每次重绘都无条件滚到底。
- 只有用户本来就在底部附近，或发出新消息这种明确需要贴底的场景，才会自动滚到底。
- 图片 / 表情包加载完成时，也只在底部附近才会补一次贴底，避免上滑看旧记录时被旧图片加载拽回去。

`v346` 优化了语音选句：

- 主动语音和显式语音都会先去掉 `（）`、`()`、`【】`、`[]`、`*...*` 里的动作描写。
- 如果候选内容本身只是动作描写，就不会生成语音，避免把“抱住你/摸摸头”这类旁白读出来。
- 普通文字气泡不受影响，仍然按原回复展示。

`v271` 把工具提示继续往“旁白/注释”方向收了一圈：

- 字号从 `11px` 继续压到 `10px`。
- 颜色从 `0.48` 收到 `0.42`，更接近时间戳那类轻提示。
- 占位高度和底部留白也再缩了一点，不会像一条独立消息那么醒目。

`v270` 把工具提示进一步对齐到了顶部状态的观感：

- 颜色本来就和状态一致，这次把字号精确对齐到了 `11px`（`0.6875rem`）。
- 同时把行高收成了 `1`，减少它看起来“比状态大一圈”的感觉。

`v269` 补上了一个漏改的版本引用：

- `index.html` 里样式表链接之前一直还停在 `styles.css?v=262`，这会让页面继续吃旧 CSS。
- 现在样式表版本也同步到了 `v269`。

`v268` 修了本地预览经常还在吃旧版本 HTML 的问题：

- 根因是 Service Worker 对 `index.html` 也走了缓存优先，导致即使地址栏版本号变了，页面里还可能引用旧的 `styles.css?v=262`。
- 现在把 HTML / 导航请求改成了网络优先，拿不到网络时才回退缓存。
- 这样本地预览看新改动会稳定很多，不容易再出现“明明改了但页面没变”的错觉。

`v267` 继续把工具调用提示收小了一圈：

- 工具提示字号从 `0.76rem` 压到了 `0.68rem`。
- 行高和上下留白也一起缩了，更接近时间戳/动作描述那种轻文字。

`v266` 把工具调用提示进一步对齐到了旧动作描述样式：

- 工具提示不再走自己那套 `tool row` 间距。
- 现在直接复用旧的 `action` 布局，视觉上更接近之前讨论过的“拍一拍 / 动作描述”效果。

`v265` 调整了工具调用的前端提示样式：

- 不再用 toast 提示“这条会先走网页搜索”。
- 私聊页底部那行“网页搜索已启用……”也去掉了。
- 现在改成聊天流里的居中小字，像微信拍一拍那样显示，例如：`调用工具：网页搜索`。
- 搜索完成后会更新成 `调用工具：网页搜索（已返回 1 条结果）`；失败时也会直接写在这行里。

`v264` 把网页搜索继续收紧了一版：

- OpenRouter `web` plugin 的 `max_results` 从 `5` 压到了 `1`。
- 这样每次命中搜索时，只会拿 1 条搜索结果给模型，先尽量止血。

`v263` 接了第一版工具调用前端和 OpenRouter 网页搜索：

- API 页新增了“工具调用”卡片，目前先只有 `网页搜索` 开关。
- 聊天输入框下方会显示当前网页搜索状态提示。
- 网页搜索默认不开；开了以后，也只有用户明确要求“搜一下 / 看看这个网页”时才会触发。
- 当前只接了 OpenRouter 的 `web` plugin；如果 base URL 不是 OpenRouter，会退回普通聊天并提示。

`v262` 优化了私聊 prompt 结构：

- 私聊最近聊天不再按 30 条消息走，改成最近 10 到 12 轮对话。
- 移除了 prompt 里的 AI 状态。
- 移除了“动作单独规则”那句保险提示。
- Todo / 共读摘要改成有才带；默认 demo 书不会再进私聊上下文。
- 长期 Memory 改成更严格的命中式调用，不再固定塞一组高优先级记忆。

`v261` 把 Journal 自动整理阈值改成了 50 条聊天：

- 现在不是 40 条，而是 50 条用户/AI 普通消息后才会自动整理一次 Journal。
- 这样能兼容 AI 分成多条气泡回复，减少过于频繁的整理。

`v260` 把 Profile 页的名字收进了“人设”二级页：

- `Profile` 首页不再直接显示名字输入和“保存名字”按钮。
- 名字输入现在放到“人设”二级页顶部，和人设正文一起维护。

`v259` 把表情面板的滚动条隐藏了：

- 表情面板仍然可以上下滑动。
- 只是把可见滚动条去掉，界面会更干净。

`v258` 给私聊表情面板加了纵向滚动：

- 表情面板现在有最大高度，超出后会在面板内部上下滚动。
- 这样后面继续加表情，不用再一直压缩单个格子大小。

`v257` 调整了私聊表情面板的排版：

- 表情列表改成一排 4 个。
- 单个表情块稍微收小了一点，面板内边距和间距也一起缩了。

`v256` 把新加的表情包接进了私聊列表：

- 已把 `image/` 里新增的几张 jpg 补进 `STICKERS` 名单。
- 私聊表情面板和模型可用表情名提示会一起更新。

`v255` 收起了私聊里的删除 / 重写入口：

- 删除和重写按钮默认不再常驻显示。
- 现在改成点开某个聊天气泡后，才在这条消息下方显示操作按钮。
- 再点一次同一条气泡，或点到别处，会收起按钮。

`v254` 修正了“分气泡影响说话风格”的上下文问题：

- 现在前端仍然会按句号把 AI 回复显示成多条气泡。
- 但发给模型的最近聊天上下文，会把同一轮 AI 的分泡重新合并成原本的一整条回复。
- 这样保留展示效果，不会慢慢把模型带成“必须一句一条”的说话习惯。

`v253` 去掉了 Moment 发布弹窗里的 placeholder：

- 发布动态弹窗里的文本框不再显示提示字。

`v252` 把 Moment 页的“发布动态”入口收进了顶栏：

- 底部的“发布动态”按钮已移除。
- 现在改成顶栏里的 `+` icon，点击后打开同一个发布弹窗。

`v251` 把用户在 Moment 页的发布入口改成了弹窗：

- 原来底部常驻的小文本框被移除。
- 现在改成底部一个“发布动态”入口按钮，点击后弹出输入框再填写发布。
- 发布成功后会自动收起弹窗。

`v250` 撤回了私聊动作提示条的强制拆分：

- system prompt 不再要求模型把动作单独写成 `[action:...]`。
- 前端也不再主动把动作拆成单独一条消息；以后就按普通聊天气泡直接展示。
- 如果模型偶尔还输出旧格式 `[action:...]`，前端会尽量把里面的内容当普通正文显示出来。

`v249` 给私聊 AI 回复加了分气泡逻辑：

- 私聊里 AI 的文字回复现在会优先按中文句号 `。` 拆成多条气泡。
- 动作提示条、表情包、识图回复、重写消息都会一起吃到这套拆分逻辑。

`v248` 调整了私聊动作描述的展示方式：

- 动作提示条不再自动在前面拼接 AI 名字或“你”。
- 现在是模型写什么就展示什么，更接近微信那种纯提示条效果。

`v247` 调整了私聊动作描述的小字颜色：

- 动作描述从 `rgba(39, 35, 31, 0.42)` 提到 `rgba(39, 35, 31, 0.48)`。
- 现在和顶栏状态字的颜色一致，不会显得太淡。

`v246` 修正了 Home 页底色被白底规则盖掉的问题：

- 根因是 `.view:not(#chat-view)` 的选择器优先级比单独的 `#home-view` 更高。
- 现在改成 `.view#home-view`，确保 Home 计算后的背景真的是 `#f7f7f7`。

`v245` 修正了 Home 页卡片缝隙发白的问题：

- 原因是 Home 视图本身透明，卡片间隙没有直接铺底色。
- 现在 `#home-view` 自己直接铺成 `#f7f7f7`，卡片之间不会再漏白。

`v244` 统一了 Home 页的背景和描边：

- Home 激活时，外层页面底色也改成和 Profile / API 一样的 `#f7f7f7`。
- Home 视图本身改为透明，让外层底色稳定露出来。
- 顶部头像大卡片补上了和其他卡片一致的浅描边。

`v243` 统一了 API 页的描边颜色：

- API 页的输入框默认描边改成和 Profile 入口卡片一致。
- API 页顶部的直连 / 代理分段切换描边也同步成同一套颜色。

`v242` 统一了 Profile 区域的描边颜色：

- 头像卡片描边改成和下方三张入口卡片一致。
- 名字输入框默认描边也改成同一套颜色。

`v241` 简化了 Profile 头像卡片的入口：

- 删掉了两张头像卡片里的“更换头像”文字按钮。
- 现在只保留点头像更换，底部继续保留“移除”。

`v240` 把设置页底色值改回了 `#f7f7f7`：

- 保留 `v239` 那套正确的层级结构：设置页透明，露出外层底色。
- 只把外层底色从 `#f3f3f3` 调回了用户最初指定的 `#f7f7f7`。

`v239` 改成了更稳的设置页底色方案：

- 不再让 `Profile / 人设 / 风格 / Memory / API` 这些设置页自己带白底。
- 这些设置页现在强制透明，把外层 `view-stack` 的浅灰底露出来。
- 卡片本身继续保持白色，所以层次会稳定很多。

`v238` 继续修正了 Profile 区域底色的可见性：

- 原因是 `#f7f7f7` 和白卡片对比太低，虽然技术上生效了，但视觉上几乎看不出来。
- 现在把设置页底色压深到 `#f3f3f3`，卡片继续保持白色，层次会更明确。

`v237` 继续修正了 Profile 区域底色的外层承载层：

- 问题原因是 `Profile` 视图本身改了，但外层 `view-stack` 还是白色，所以视觉上不明显。
- 现在当 `Profile / 人设 / 风格 / Memory / API` 任一设置页激活时，外层承载层也会一起切到 `#f7f7f7`。

`v236` 继续修正了 Profile 区域底色的生效层级：

- 不再只改设置页内部滚动容器。
- 现在是直接把 `Profile / 人设 / 风格 / Memory / API` 这些视图层本身铺成 `#f7f7f7`，视觉上会更明显。

`v235` 调整了 Profile 区域的页面底色：

- `Profile / 人设 / 风格 / Memory / API` 这组设置页的背景改成了和顶栏一致的 `#f7f7f7`。
- 卡片本身的白色没有动。

`v234` 微调了动作提示条的纵向位置：

- 给动作提示条单独留了更明确的纵向空间。
- 让动作小字在上下两条消息之间更像“待在空档正中间”，不再显得贴近上一条或下一条。

`v233` 继续补强了动作提示条的兜底识别：

- 如果模型老老实实输出 `[action:...]`，会正常拆成动作小字。
- 如果模型偶尔写成回复开头的 `（轻轻抱了抱你）` 这种括号动作，前端也会尽量识别并拆开。

`v232` 给私聊加了动作提示条：

- AI 如果需要表达动作，不再把动作塞进聊天气泡正文。
- 动作现在支持用 `[action:...]` 标记拆成单独一行小字，效果接近微信“拍一拍”那种提示条。
- 正常说的话仍然留在聊天气泡里；有动作时，会先显示动作提示条，再显示正文气泡。

`v231` 给私聊补了轻量 Home 摘要：

- 私聊主链路、图片私聊链路、私聊重写链路现在会额外带两句很短的上下文。
- Todo 只带一句概括，例如“今天待办：练背、买眼药水，还有 1 条没完成。”
- 共读只带一句进度，例如“最近在读《夜色温柔练习册》，读到第 2 段。”
- 这两句不会接到 Moment / Diary / 共读批注这些别的功能链路里。

`v230` 微调了 Home 页 Todo 的加号入口样式：

- 去掉了加号按钮底色。
- 缩小了按钮占位，让它更贴近 `Todo` 标题这一行的视觉尺寸。

`v229` 调整了 Home 页 Todo 的添加方式：

- 去掉卡片里常驻的输入框。
- 在 `Todo` 标题右上角加了 icon 按钮作为新增入口。
- 点击后会弹出输入弹窗，再填写并添加待办。

`v228` 在 Home 页面新增了 Todo list：

- 位置放在天气卡片和便签卡片中间。
- 支持直接新增、勾选完成、删除，数据保存在本地状态里。
- 样式沿用 Home 现有卡片体系，没有单独跳出一套新视觉。

`v227` 继续微调了 Home 里的天气卡片：

- 保留 `v225` 的布局。
- 把天气卡片的 icon 图形和 icon 小底块尺寸，恢复到更接近原来 Home 卡片那套样式。

`v226` 微调了 Home 里的天气卡片配色：

- 保留了 `v225` 的新布局。
- 去掉渐变和额外阴影，把卡片底色、icon 底色都收回到页面原来的配色体系里。

`v225` 优化了 Home 里的天气卡片样式：

- 天气卡片改成了更完整的上下布局，图标和温度单独成一层，城市和说明文案放到底部，层次更清楚。
- 原来的定位 pin icon 改成了天气 icon。
- 未配置、待补 Host、已有天气数据三种状态现在会分别显示，首页观感更稳定。

`v224` 继续收口了 Home 页面内容：

- 完全去掉了每日歌曲这块内容，不再显示，也不再请求模型生成歌曲。
- 天气卡片移动到了原来播放器所在的位置，Home 页面只保留一张天气卡片。

`v222` 继续简化了 Profile 二级页：

- `人设` 和 `对话风格参考` 二级页的解释卡片都删掉了。
- 现在只保留标题、文本框和底部保存按钮。

`v221` 调整了 Profile 里卡片图标：

- `人设` 入口改成了 circle-user-round icon。
- `对话风格参考` 入口改成了 messages-square icon。
- `Memory 库` 入口继续使用 bubbles icon，颜色都沿用页面现有 icon 颜色。

`v219` 继续清理了 Memory 页说明文案：

- 删掉了 Memory 页顶部那句较长的说明文字，只保留标题和统计信息。

`v218` 进一步统一了返回交互：

- 删掉了 `Memory` 卡片下面的提示文案。
- `人设 / 对话风格参考 / Memory` 二级页不再在页面内容里放返回按钮，统一改用顶栏返回 icon。
- Home 二级页和 Profile 二级页现在共用同一套返回方式。

`v217` 微调了 Profile 卡片样式：

- `人设 / Memory 库 / 对话风格参考` 三张入口卡片现在都只保留标题，不显示副文案。
- 三张卡片的视觉层级统一了。

`v216` 调整了 Profile 的编辑结构：

- `人设` 和 `对话风格参考` 现在都改成了 Profile 里的独立入口卡片。
- 点进二级页面后再编辑文本框，保存按钮放在文本框底部。
- `Memory 库` 保持二级页面结构不变。

`v215` 调整了 Moment 评论的人格连续性：

- Moment 评论和评论区二次回复现在会更强调“和私聊是同一个人”，优先保证人格连续，再考虑评论场景。
- Moment 评论链路不再带最近 2 条 Journal，避免评论区语气被摘要感带跑。

`v214` 调整了 Moment 的上下文逻辑：

- Moment 发布时不再带最近 2 条 Journal，避免更像总结而不是随手发的一句。
- 普通私聊仍然保留最近 2 条 Journal 的上下文逻辑。

`v213` 新增了结构化 Memory / Journal 系统：

- Profile 里新增 `Memory 库` 入口，旧的大段 Memory / Journal 维护方式已退场。
- Journal 仍然按 50 条聊天自动整理。
- Journal 生成后会继续自动判断：只留在 Journal、写入长期 Memory、或写入重要经历。
- 结构化 Memory 会按当前聊天主题动态取用，不再每次都塞整段旧 Memory。
